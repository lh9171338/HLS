// ==============================================================
// File generated by Vivado(TM) HLS - High-Level Synthesis from C, C++ and SystemC
// Version: 2018.2
// Copyright (C) 1986-2018 Xilinx, Inc. All Rights Reserved.
// 
// ==============================================================


`timescale 1 ns / 1 ps

`define TV_IN_src_axis_TDATA "./c.HoughLines_Core.autotvin_src_axis_V_data_V.dat"
`define INGRESS_STATUS_src_axis_TDATA "./stream_ingress_status_src_axis_V_data_V.dat"
`define TV_IN_src_axis_TKEEP "./c.HoughLines_Core.autotvin_src_axis_V_keep_V.dat"
`define INGRESS_STATUS_src_axis_TKEEP "./stream_ingress_status_src_axis_V_keep_V.dat"
`define TV_IN_src_axis_TSTRB "./c.HoughLines_Core.autotvin_src_axis_V_strb_V.dat"
`define INGRESS_STATUS_src_axis_TSTRB "./stream_ingress_status_src_axis_V_strb_V.dat"
`define TV_IN_src_axis_TUSER "./c.HoughLines_Core.autotvin_src_axis_V_user_V.dat"
`define INGRESS_STATUS_src_axis_TUSER "./stream_ingress_status_src_axis_V_user_V.dat"
`define TV_IN_src_axis_TLAST "./c.HoughLines_Core.autotvin_src_axis_V_last_V.dat"
`define INGRESS_STATUS_src_axis_TLAST "./stream_ingress_status_src_axis_V_last_V.dat"
`define TV_IN_src_axis_TID "./c.HoughLines_Core.autotvin_src_axis_V_id_V.dat"
`define INGRESS_STATUS_src_axis_TID "./stream_ingress_status_src_axis_V_id_V.dat"
`define TV_IN_src_axis_TDEST "./c.HoughLines_Core.autotvin_src_axis_V_dest_V.dat"
`define INGRESS_STATUS_src_axis_TDEST "./stream_ingress_status_src_axis_V_dest_V.dat"

`define AUTOTB_TRANSACTION_NUM 1

module AESL_axi_s_src_axis (
    input clk,
    input reset,
    output [8 - 1:0] TRAN_src_axis_TDATA,
    output TRAN_src_axis_TKEEP,
    output TRAN_src_axis_TSTRB,
    output TRAN_src_axis_TUSER,
    output TRAN_src_axis_TLAST,
    output TRAN_src_axis_TID,
    output TRAN_src_axis_TDEST,
    output TRAN_src_axis_TVALID,
    input TRAN_src_axis_TREADY,
    input ready,
    input done,
    output [31:0] transaction);

    wire TRAN_src_axis_TVALID_temp;
    wire src_axis_TDATA_full;
    wire src_axis_TDATA_empty;
    reg src_axis_TDATA_write_en;
    reg [8 - 1:0] src_axis_TDATA_write_data;
    reg src_axis_TDATA_read_en;
    wire [8 - 1:0] src_axis_TDATA_read_data;
    
    fifo #(75763, 8) fifo_src_axis_TDATA (
        .reset(1'b0),
        .write_clock(clk),
        .write_en(src_axis_TDATA_write_en),
        .write_data(src_axis_TDATA_write_data),
        .read_clock(clk),
        .read_en(src_axis_TDATA_read_en),
        .read_data(src_axis_TDATA_read_data),
        .full(src_axis_TDATA_full),
        .empty(src_axis_TDATA_empty));
    
    always @ (*) begin
        src_axis_TDATA_write_en <= 0;
        src_axis_TDATA_read_en <= TRAN_src_axis_TREADY & TRAN_src_axis_TVALID;
    end
    
    assign TRAN_src_axis_TDATA = src_axis_TDATA_read_data;
    wire src_axis_TKEEP_full;
    wire src_axis_TKEEP_empty;
    reg src_axis_TKEEP_write_en;
    reg [1 - 1:0] src_axis_TKEEP_write_data;
    reg src_axis_TKEEP_read_en;
    wire [1 - 1:0] src_axis_TKEEP_read_data;
    
    fifo #(75763, 1) fifo_src_axis_TKEEP (
        .reset(1'b0),
        .write_clock(clk),
        .write_en(src_axis_TKEEP_write_en),
        .write_data(src_axis_TKEEP_write_data),
        .read_clock(clk),
        .read_en(src_axis_TKEEP_read_en),
        .read_data(src_axis_TKEEP_read_data),
        .full(src_axis_TKEEP_full),
        .empty(src_axis_TKEEP_empty));
    
    always @ (*) begin
        src_axis_TKEEP_write_en <= 0;
        src_axis_TKEEP_read_en <= TRAN_src_axis_TREADY & TRAN_src_axis_TVALID;
    end
    
    assign TRAN_src_axis_TKEEP = src_axis_TKEEP_read_data;
    wire src_axis_TSTRB_full;
    wire src_axis_TSTRB_empty;
    reg src_axis_TSTRB_write_en;
    reg [1 - 1:0] src_axis_TSTRB_write_data;
    reg src_axis_TSTRB_read_en;
    wire [1 - 1:0] src_axis_TSTRB_read_data;
    
    fifo #(75763, 1) fifo_src_axis_TSTRB (
        .reset(1'b0),
        .write_clock(clk),
        .write_en(src_axis_TSTRB_write_en),
        .write_data(src_axis_TSTRB_write_data),
        .read_clock(clk),
        .read_en(src_axis_TSTRB_read_en),
        .read_data(src_axis_TSTRB_read_data),
        .full(src_axis_TSTRB_full),
        .empty(src_axis_TSTRB_empty));
    
    always @ (*) begin
        src_axis_TSTRB_write_en <= 0;
        src_axis_TSTRB_read_en <= TRAN_src_axis_TREADY & TRAN_src_axis_TVALID;
    end
    
    assign TRAN_src_axis_TSTRB = src_axis_TSTRB_read_data;
    wire src_axis_TUSER_full;
    wire src_axis_TUSER_empty;
    reg src_axis_TUSER_write_en;
    reg [1 - 1:0] src_axis_TUSER_write_data;
    reg src_axis_TUSER_read_en;
    wire [1 - 1:0] src_axis_TUSER_read_data;
    
    fifo #(75763, 1) fifo_src_axis_TUSER (
        .reset(1'b0),
        .write_clock(clk),
        .write_en(src_axis_TUSER_write_en),
        .write_data(src_axis_TUSER_write_data),
        .read_clock(clk),
        .read_en(src_axis_TUSER_read_en),
        .read_data(src_axis_TUSER_read_data),
        .full(src_axis_TUSER_full),
        .empty(src_axis_TUSER_empty));
    
    always @ (*) begin
        src_axis_TUSER_write_en <= 0;
        src_axis_TUSER_read_en <= TRAN_src_axis_TREADY & TRAN_src_axis_TVALID;
    end
    
    assign TRAN_src_axis_TUSER = src_axis_TUSER_read_data;
    wire src_axis_TLAST_full;
    wire src_axis_TLAST_empty;
    reg src_axis_TLAST_write_en;
    reg [1 - 1:0] src_axis_TLAST_write_data;
    reg src_axis_TLAST_read_en;
    wire [1 - 1:0] src_axis_TLAST_read_data;
    
    fifo #(75763, 1) fifo_src_axis_TLAST (
        .reset(1'b0),
        .write_clock(clk),
        .write_en(src_axis_TLAST_write_en),
        .write_data(src_axis_TLAST_write_data),
        .read_clock(clk),
        .read_en(src_axis_TLAST_read_en),
        .read_data(src_axis_TLAST_read_data),
        .full(src_axis_TLAST_full),
        .empty(src_axis_TLAST_empty));
    
    always @ (*) begin
        src_axis_TLAST_write_en <= 0;
        src_axis_TLAST_read_en <= TRAN_src_axis_TREADY & TRAN_src_axis_TVALID;
    end
    
    assign TRAN_src_axis_TLAST = src_axis_TLAST_read_data;
    wire src_axis_TID_full;
    wire src_axis_TID_empty;
    reg src_axis_TID_write_en;
    reg [1 - 1:0] src_axis_TID_write_data;
    reg src_axis_TID_read_en;
    wire [1 - 1:0] src_axis_TID_read_data;
    
    fifo #(75763, 1) fifo_src_axis_TID (
        .reset(1'b0),
        .write_clock(clk),
        .write_en(src_axis_TID_write_en),
        .write_data(src_axis_TID_write_data),
        .read_clock(clk),
        .read_en(src_axis_TID_read_en),
        .read_data(src_axis_TID_read_data),
        .full(src_axis_TID_full),
        .empty(src_axis_TID_empty));
    
    always @ (*) begin
        src_axis_TID_write_en <= 0;
        src_axis_TID_read_en <= TRAN_src_axis_TREADY & TRAN_src_axis_TVALID;
    end
    
    assign TRAN_src_axis_TID = src_axis_TID_read_data;
    wire src_axis_TDEST_full;
    wire src_axis_TDEST_empty;
    reg src_axis_TDEST_write_en;
    reg [1 - 1:0] src_axis_TDEST_write_data;
    reg src_axis_TDEST_read_en;
    wire [1 - 1:0] src_axis_TDEST_read_data;
    
    fifo #(75763, 1) fifo_src_axis_TDEST (
        .reset(1'b0),
        .write_clock(clk),
        .write_en(src_axis_TDEST_write_en),
        .write_data(src_axis_TDEST_write_data),
        .read_clock(clk),
        .read_en(src_axis_TDEST_read_en),
        .read_data(src_axis_TDEST_read_data),
        .full(src_axis_TDEST_full),
        .empty(src_axis_TDEST_empty));
    
    always @ (*) begin
        src_axis_TDEST_write_en <= 0;
        src_axis_TDEST_read_en <= TRAN_src_axis_TREADY & TRAN_src_axis_TVALID;
    end
    
    assign TRAN_src_axis_TDEST = src_axis_TDEST_read_data;
    assign TRAN_src_axis_TVALID = TRAN_src_axis_TVALID_temp;

    
    reg src_axis_TDATA_valid = 0; // ingress buffer indicator: src_axis_TDATA
    reg src_axis_TKEEP_valid = 0; // ingress buffer indicator: src_axis_TKEEP
    reg src_axis_TSTRB_valid = 0; // ingress buffer indicator: src_axis_TSTRB
    reg src_axis_TUSER_valid = 0; // ingress buffer indicator: src_axis_TUSER
    reg src_axis_TLAST_valid = 0; // ingress buffer indicator: src_axis_TLAST
    reg src_axis_TID_valid = 0; // ingress buffer indicator: src_axis_TID
    reg src_axis_TDEST_valid = 0; // ingress buffer indicator: src_axis_TDEST
    
    assign TRAN_src_axis_TVALID_temp = ~(src_axis_TDATA_empty || src_axis_TKEEP_empty || src_axis_TSTRB_empty || src_axis_TUSER_empty || src_axis_TLAST_empty || src_axis_TID_empty || src_axis_TDEST_empty) || (src_axis_TDATA_valid && src_axis_TKEEP_valid && src_axis_TSTRB_valid && src_axis_TUSER_valid && src_axis_TLAST_valid && src_axis_TID_valid && src_axis_TDEST_valid);
    
    function is_blank_char(input [7:0] in_char);
        if (in_char == " " || in_char == "\011" || in_char == "\012" || in_char == "\015") begin
            is_blank_char = 1;
        end else begin
            is_blank_char = 0;
        end
    endfunction
    
    function [167:0] read_token(input integer fp);
        integer ret;
        begin
            read_token = "";
                    ret = 0;
                    ret = $fscanf(fp,"%s",read_token);
        end
    endfunction
    
    function [167:0] rm_0x(input [167:0] token);
        reg [167:0] token_tmp;
        integer i;
        begin
            token_tmp = "";
            for (i = 0; token[15:0] != "0x"; token = token >> 8) begin
                token_tmp = (token[7:0] << (8 * i)) | token_tmp;
                i = i + 1;
            end
            rm_0x = token_tmp;
        end
    endfunction
    
    reg [31:0] transaction_load_src_axis_TDATA;
    
    assign transaction = transaction_load_src_axis_TDATA;
    
    initial begin : AXI_stream_driver_src_axis_TDATA
        integer fp;
        reg [167:0] token;
        reg [8 - 1:0] data;
        reg [167:0] data_integer;
        integer fp_ingress_status;
        reg [167:0] token_ingress_status;
        reg [31:0] ingress_status;
        reg [8 * 5:1] str;
        integer ret;
        
        transaction_load_src_axis_TDATA = 0;
        fifo_src_axis_TDATA.clear();
        wait (reset === 1);
        fp = $fopen(`TV_IN_src_axis_TDATA, "r");
        if (fp == 0) begin // Failed to open file
            $display("ERROR: Failed to open file \"%s\"!", `TV_IN_src_axis_TDATA);
            $finish;
        end
        fp_ingress_status = $fopen(`INGRESS_STATUS_src_axis_TDATA, "r");
        if (fp_ingress_status == 0) begin // Failed to open file
            $display("ERROR: Failed to open file \"%s\"!", `INGRESS_STATUS_src_axis_TDATA);
            $finish;
        end
        token = read_token(fp);
        token_ingress_status = read_token(fp_ingress_status);
        if (token != "[[[runtime]]]") begin
            $display("ERROR: token %s != [[[runtime]]]", token);
            $finish;
        end
        token = read_token(fp); // read 1st "[[transaction]]"
        token_ingress_status = read_token(fp_ingress_status);
        forever begin
            @ (negedge clk);
            if (ready == 1) begin
                if (token != "[[[/runtime]]]") begin
                    if (token != "[[transaction]]") begin
                        $display("ERROR: token %s != [[[transaction]]]", token);
                        $finish;
                    end
                    token = read_token(fp); // skip transaction number
                    token_ingress_status = read_token(fp_ingress_status); // skip transaction number
                    token_ingress_status = read_token(fp_ingress_status); // load ingress status at beginning of transaction
                    fifo_src_axis_TDATA.clear();
                    token = read_token(fp);
                    ret = $sscanf(token_ingress_status, "%d", ingress_status);
                    token_ingress_status = read_token(fp_ingress_status);
                    while (token != "[[/transaction]]") begin
                        if (fifo_src_axis_TDATA.full) begin
                            $display("ERROR: FIFO is full");
                            $finish;
                        end
                        ret = $sscanf(rm_0x(token), "%x", data_integer);
                        data = data_integer;
                        fifo_src_axis_TDATA.push(data);
                        token = read_token(fp);
                        ret = $sscanf(token_ingress_status, "%d", ingress_status);
                        token_ingress_status = read_token(fp_ingress_status);
                    end
                    src_axis_TDATA_valid = (ingress_status > 0);
                    token = read_token(fp);
                    token_ingress_status = read_token(fp_ingress_status);
                    fifo_src_axis_TDATA.snapshot();
                end else begin
                    fifo_src_axis_TDATA.restore();
                    if (fp != 0) begin
                        $fclose(fp);
                        fp = 0;
                    end
                    if (fp_ingress_status != 0) begin
                        $fclose(fp_ingress_status);
                        fp_ingress_status = 0;
                    end
                end
                transaction_load_src_axis_TDATA = transaction_load_src_axis_TDATA + 1;
            end
        end
    end
    
    reg [31:0] transaction_load_src_axis_TKEEP;
    
    initial begin : AXI_stream_driver_src_axis_TKEEP
        integer fp;
        reg [167:0] token;
        reg [1 - 1:0] data;
        reg [167:0] data_integer;
        integer fp_ingress_status;
        reg [167:0] token_ingress_status;
        reg [31:0] ingress_status;
        reg [8 * 5:1] str;
        integer ret;
        
        transaction_load_src_axis_TKEEP = 0;
        fifo_src_axis_TKEEP.clear();
        wait (reset === 1);
        fp = $fopen(`TV_IN_src_axis_TKEEP, "r");
        if (fp == 0) begin // Failed to open file
            $display("ERROR: Failed to open file \"%s\"!", `TV_IN_src_axis_TKEEP);
            $finish;
        end
        fp_ingress_status = $fopen(`INGRESS_STATUS_src_axis_TKEEP, "r");
        if (fp_ingress_status == 0) begin // Failed to open file
            $display("ERROR: Failed to open file \"%s\"!", `INGRESS_STATUS_src_axis_TKEEP);
            $finish;
        end
        token = read_token(fp);
        token_ingress_status = read_token(fp_ingress_status);
        if (token != "[[[runtime]]]") begin
            $display("ERROR: token %s != [[[runtime]]]", token);
            $finish;
        end
        token = read_token(fp); // read 1st "[[transaction]]"
        token_ingress_status = read_token(fp_ingress_status);
        forever begin
            @ (negedge clk);
            if (ready == 1) begin
                if (token != "[[[/runtime]]]") begin
                    if (token != "[[transaction]]") begin
                        $display("ERROR: token %s != [[[transaction]]]", token);
                        $finish;
                    end
                    token = read_token(fp); // skip transaction number
                    token_ingress_status = read_token(fp_ingress_status); // skip transaction number
                    token_ingress_status = read_token(fp_ingress_status); // load ingress status at beginning of transaction
                    fifo_src_axis_TKEEP.clear();
                    token = read_token(fp);
                    ret = $sscanf(token_ingress_status, "%d", ingress_status);
                    token_ingress_status = read_token(fp_ingress_status);
                    while (token != "[[/transaction]]") begin
                        if (fifo_src_axis_TKEEP.full) begin
                            $display("ERROR: FIFO is full");
                            $finish;
                        end
                        ret = $sscanf(rm_0x(token), "%x", data_integer);
                        data = data_integer;
                        fifo_src_axis_TKEEP.push(data);
                        token = read_token(fp);
                        ret = $sscanf(token_ingress_status, "%d", ingress_status);
                        token_ingress_status = read_token(fp_ingress_status);
                    end
                    src_axis_TKEEP_valid = (ingress_status > 0);
                    token = read_token(fp);
                    token_ingress_status = read_token(fp_ingress_status);
                    fifo_src_axis_TKEEP.snapshot();
                end else begin
                    fifo_src_axis_TKEEP.restore();
                    if (fp != 0) begin
                        $fclose(fp);
                        fp = 0;
                    end
                    if (fp_ingress_status != 0) begin
                        $fclose(fp_ingress_status);
                        fp_ingress_status = 0;
                    end
                end
                transaction_load_src_axis_TKEEP = transaction_load_src_axis_TKEEP + 1;
            end
        end
    end
    
    reg [31:0] transaction_load_src_axis_TSTRB;
    
    initial begin : AXI_stream_driver_src_axis_TSTRB
        integer fp;
        reg [167:0] token;
        reg [1 - 1:0] data;
        reg [167:0] data_integer;
        integer fp_ingress_status;
        reg [167:0] token_ingress_status;
        reg [31:0] ingress_status;
        reg [8 * 5:1] str;
        integer ret;
        
        transaction_load_src_axis_TSTRB = 0;
        fifo_src_axis_TSTRB.clear();
        wait (reset === 1);
        fp = $fopen(`TV_IN_src_axis_TSTRB, "r");
        if (fp == 0) begin // Failed to open file
            $display("ERROR: Failed to open file \"%s\"!", `TV_IN_src_axis_TSTRB);
            $finish;
        end
        fp_ingress_status = $fopen(`INGRESS_STATUS_src_axis_TSTRB, "r");
        if (fp_ingress_status == 0) begin // Failed to open file
            $display("ERROR: Failed to open file \"%s\"!", `INGRESS_STATUS_src_axis_TSTRB);
            $finish;
        end
        token = read_token(fp);
        token_ingress_status = read_token(fp_ingress_status);
        if (token != "[[[runtime]]]") begin
            $display("ERROR: token %s != [[[runtime]]]", token);
            $finish;
        end
        token = read_token(fp); // read 1st "[[transaction]]"
        token_ingress_status = read_token(fp_ingress_status);
        forever begin
            @ (negedge clk);
            if (ready == 1) begin
                if (token != "[[[/runtime]]]") begin
                    if (token != "[[transaction]]") begin
                        $display("ERROR: token %s != [[[transaction]]]", token);
                        $finish;
                    end
                    token = read_token(fp); // skip transaction number
                    token_ingress_status = read_token(fp_ingress_status); // skip transaction number
                    token_ingress_status = read_token(fp_ingress_status); // load ingress status at beginning of transaction
                    fifo_src_axis_TSTRB.clear();
                    token = read_token(fp);
                    ret = $sscanf(token_ingress_status, "%d", ingress_status);
                    token_ingress_status = read_token(fp_ingress_status);
                    while (token != "[[/transaction]]") begin
                        if (fifo_src_axis_TSTRB.full) begin
                            $display("ERROR: FIFO is full");
                            $finish;
                        end
                        ret = $sscanf(rm_0x(token), "%x", data_integer);
                        data = data_integer;
                        fifo_src_axis_TSTRB.push(data);
                        token = read_token(fp);
                        ret = $sscanf(token_ingress_status, "%d", ingress_status);
                        token_ingress_status = read_token(fp_ingress_status);
                    end
                    src_axis_TSTRB_valid = (ingress_status > 0);
                    token = read_token(fp);
                    token_ingress_status = read_token(fp_ingress_status);
                    fifo_src_axis_TSTRB.snapshot();
                end else begin
                    fifo_src_axis_TSTRB.restore();
                    if (fp != 0) begin
                        $fclose(fp);
                        fp = 0;
                    end
                    if (fp_ingress_status != 0) begin
                        $fclose(fp_ingress_status);
                        fp_ingress_status = 0;
                    end
                end
                transaction_load_src_axis_TSTRB = transaction_load_src_axis_TSTRB + 1;
            end
        end
    end
    
    reg [31:0] transaction_load_src_axis_TUSER;
    
    initial begin : AXI_stream_driver_src_axis_TUSER
        integer fp;
        reg [167:0] token;
        reg [1 - 1:0] data;
        reg [167:0] data_integer;
        integer fp_ingress_status;
        reg [167:0] token_ingress_status;
        reg [31:0] ingress_status;
        reg [8 * 5:1] str;
        integer ret;
        
        transaction_load_src_axis_TUSER = 0;
        fifo_src_axis_TUSER.clear();
        wait (reset === 1);
        fp = $fopen(`TV_IN_src_axis_TUSER, "r");
        if (fp == 0) begin // Failed to open file
            $display("ERROR: Failed to open file \"%s\"!", `TV_IN_src_axis_TUSER);
            $finish;
        end
        fp_ingress_status = $fopen(`INGRESS_STATUS_src_axis_TUSER, "r");
        if (fp_ingress_status == 0) begin // Failed to open file
            $display("ERROR: Failed to open file \"%s\"!", `INGRESS_STATUS_src_axis_TUSER);
            $finish;
        end
        token = read_token(fp);
        token_ingress_status = read_token(fp_ingress_status);
        if (token != "[[[runtime]]]") begin
            $display("ERROR: token %s != [[[runtime]]]", token);
            $finish;
        end
        token = read_token(fp); // read 1st "[[transaction]]"
        token_ingress_status = read_token(fp_ingress_status);
        forever begin
            @ (negedge clk);
            if (ready == 1) begin
                if (token != "[[[/runtime]]]") begin
                    if (token != "[[transaction]]") begin
                        $display("ERROR: token %s != [[[transaction]]]", token);
                        $finish;
                    end
                    token = read_token(fp); // skip transaction number
                    token_ingress_status = read_token(fp_ingress_status); // skip transaction number
                    token_ingress_status = read_token(fp_ingress_status); // load ingress status at beginning of transaction
                    fifo_src_axis_TUSER.clear();
                    token = read_token(fp);
                    ret = $sscanf(token_ingress_status, "%d", ingress_status);
                    token_ingress_status = read_token(fp_ingress_status);
                    while (token != "[[/transaction]]") begin
                        if (fifo_src_axis_TUSER.full) begin
                            $display("ERROR: FIFO is full");
                            $finish;
                        end
                        ret = $sscanf(rm_0x(token), "%x", data_integer);
                        data = data_integer;
                        fifo_src_axis_TUSER.push(data);
                        token = read_token(fp);
                        ret = $sscanf(token_ingress_status, "%d", ingress_status);
                        token_ingress_status = read_token(fp_ingress_status);
                    end
                    src_axis_TUSER_valid = (ingress_status > 0);
                    token = read_token(fp);
                    token_ingress_status = read_token(fp_ingress_status);
                    fifo_src_axis_TUSER.snapshot();
                end else begin
                    fifo_src_axis_TUSER.restore();
                    if (fp != 0) begin
                        $fclose(fp);
                        fp = 0;
                    end
                    if (fp_ingress_status != 0) begin
                        $fclose(fp_ingress_status);
                        fp_ingress_status = 0;
                    end
                end
                transaction_load_src_axis_TUSER = transaction_load_src_axis_TUSER + 1;
            end
        end
    end
    
    reg [31:0] transaction_load_src_axis_TLAST;
    
    initial begin : AXI_stream_driver_src_axis_TLAST
        integer fp;
        reg [167:0] token;
        reg [1 - 1:0] data;
        reg [167:0] data_integer;
        integer fp_ingress_status;
        reg [167:0] token_ingress_status;
        reg [31:0] ingress_status;
        reg [8 * 5:1] str;
        integer ret;
        
        transaction_load_src_axis_TLAST = 0;
        fifo_src_axis_TLAST.clear();
        wait (reset === 1);
        fp = $fopen(`TV_IN_src_axis_TLAST, "r");
        if (fp == 0) begin // Failed to open file
            $display("ERROR: Failed to open file \"%s\"!", `TV_IN_src_axis_TLAST);
            $finish;
        end
        fp_ingress_status = $fopen(`INGRESS_STATUS_src_axis_TLAST, "r");
        if (fp_ingress_status == 0) begin // Failed to open file
            $display("ERROR: Failed to open file \"%s\"!", `INGRESS_STATUS_src_axis_TLAST);
            $finish;
        end
        token = read_token(fp);
        token_ingress_status = read_token(fp_ingress_status);
        if (token != "[[[runtime]]]") begin
            $display("ERROR: token %s != [[[runtime]]]", token);
            $finish;
        end
        token = read_token(fp); // read 1st "[[transaction]]"
        token_ingress_status = read_token(fp_ingress_status);
        forever begin
            @ (negedge clk);
            if (ready == 1) begin
                if (token != "[[[/runtime]]]") begin
                    if (token != "[[transaction]]") begin
                        $display("ERROR: token %s != [[[transaction]]]", token);
                        $finish;
                    end
                    token = read_token(fp); // skip transaction number
                    token_ingress_status = read_token(fp_ingress_status); // skip transaction number
                    token_ingress_status = read_token(fp_ingress_status); // load ingress status at beginning of transaction
                    fifo_src_axis_TLAST.clear();
                    token = read_token(fp);
                    ret = $sscanf(token_ingress_status, "%d", ingress_status);
                    token_ingress_status = read_token(fp_ingress_status);
                    while (token != "[[/transaction]]") begin
                        if (fifo_src_axis_TLAST.full) begin
                            $display("ERROR: FIFO is full");
                            $finish;
                        end
                        ret = $sscanf(rm_0x(token), "%x", data_integer);
                        data = data_integer;
                        fifo_src_axis_TLAST.push(data);
                        token = read_token(fp);
                        ret = $sscanf(token_ingress_status, "%d", ingress_status);
                        token_ingress_status = read_token(fp_ingress_status);
                    end
                    src_axis_TLAST_valid = (ingress_status > 0);
                    token = read_token(fp);
                    token_ingress_status = read_token(fp_ingress_status);
                    fifo_src_axis_TLAST.snapshot();
                end else begin
                    fifo_src_axis_TLAST.restore();
                    if (fp != 0) begin
                        $fclose(fp);
                        fp = 0;
                    end
                    if (fp_ingress_status != 0) begin
                        $fclose(fp_ingress_status);
                        fp_ingress_status = 0;
                    end
                end
                transaction_load_src_axis_TLAST = transaction_load_src_axis_TLAST + 1;
            end
        end
    end
    
    reg [31:0] transaction_load_src_axis_TID;
    
    initial begin : AXI_stream_driver_src_axis_TID
        integer fp;
        reg [167:0] token;
        reg [1 - 1:0] data;
        reg [167:0] data_integer;
        integer fp_ingress_status;
        reg [167:0] token_ingress_status;
        reg [31:0] ingress_status;
        reg [8 * 5:1] str;
        integer ret;
        
        transaction_load_src_axis_TID = 0;
        fifo_src_axis_TID.clear();
        wait (reset === 1);
        fp = $fopen(`TV_IN_src_axis_TID, "r");
        if (fp == 0) begin // Failed to open file
            $display("ERROR: Failed to open file \"%s\"!", `TV_IN_src_axis_TID);
            $finish;
        end
        fp_ingress_status = $fopen(`INGRESS_STATUS_src_axis_TID, "r");
        if (fp_ingress_status == 0) begin // Failed to open file
            $display("ERROR: Failed to open file \"%s\"!", `INGRESS_STATUS_src_axis_TID);
            $finish;
        end
        token = read_token(fp);
        token_ingress_status = read_token(fp_ingress_status);
        if (token != "[[[runtime]]]") begin
            $display("ERROR: token %s != [[[runtime]]]", token);
            $finish;
        end
        token = read_token(fp); // read 1st "[[transaction]]"
        token_ingress_status = read_token(fp_ingress_status);
        forever begin
            @ (negedge clk);
            if (ready == 1) begin
                if (token != "[[[/runtime]]]") begin
                    if (token != "[[transaction]]") begin
                        $display("ERROR: token %s != [[[transaction]]]", token);
                        $finish;
                    end
                    token = read_token(fp); // skip transaction number
                    token_ingress_status = read_token(fp_ingress_status); // skip transaction number
                    token_ingress_status = read_token(fp_ingress_status); // load ingress status at beginning of transaction
                    fifo_src_axis_TID.clear();
                    token = read_token(fp);
                    ret = $sscanf(token_ingress_status, "%d", ingress_status);
                    token_ingress_status = read_token(fp_ingress_status);
                    while (token != "[[/transaction]]") begin
                        if (fifo_src_axis_TID.full) begin
                            $display("ERROR: FIFO is full");
                            $finish;
                        end
                        ret = $sscanf(rm_0x(token), "%x", data_integer);
                        data = data_integer;
                        fifo_src_axis_TID.push(data);
                        token = read_token(fp);
                        ret = $sscanf(token_ingress_status, "%d", ingress_status);
                        token_ingress_status = read_token(fp_ingress_status);
                    end
                    src_axis_TID_valid = (ingress_status > 0);
                    token = read_token(fp);
                    token_ingress_status = read_token(fp_ingress_status);
                    fifo_src_axis_TID.snapshot();
                end else begin
                    fifo_src_axis_TID.restore();
                    if (fp != 0) begin
                        $fclose(fp);
                        fp = 0;
                    end
                    if (fp_ingress_status != 0) begin
                        $fclose(fp_ingress_status);
                        fp_ingress_status = 0;
                    end
                end
                transaction_load_src_axis_TID = transaction_load_src_axis_TID + 1;
            end
        end
    end
    
    reg [31:0] transaction_load_src_axis_TDEST;
    
    initial begin : AXI_stream_driver_src_axis_TDEST
        integer fp;
        reg [167:0] token;
        reg [1 - 1:0] data;
        reg [167:0] data_integer;
        integer fp_ingress_status;
        reg [167:0] token_ingress_status;
        reg [31:0] ingress_status;
        reg [8 * 5:1] str;
        integer ret;
        
        transaction_load_src_axis_TDEST = 0;
        fifo_src_axis_TDEST.clear();
        wait (reset === 1);
        fp = $fopen(`TV_IN_src_axis_TDEST, "r");
        if (fp == 0) begin // Failed to open file
            $display("ERROR: Failed to open file \"%s\"!", `TV_IN_src_axis_TDEST);
            $finish;
        end
        fp_ingress_status = $fopen(`INGRESS_STATUS_src_axis_TDEST, "r");
        if (fp_ingress_status == 0) begin // Failed to open file
            $display("ERROR: Failed to open file \"%s\"!", `INGRESS_STATUS_src_axis_TDEST);
            $finish;
        end
        token = read_token(fp);
        token_ingress_status = read_token(fp_ingress_status);
        if (token != "[[[runtime]]]") begin
            $display("ERROR: token %s != [[[runtime]]]", token);
            $finish;
        end
        token = read_token(fp); // read 1st "[[transaction]]"
        token_ingress_status = read_token(fp_ingress_status);
        forever begin
            @ (negedge clk);
            if (ready == 1) begin
                if (token != "[[[/runtime]]]") begin
                    if (token != "[[transaction]]") begin
                        $display("ERROR: token %s != [[[transaction]]]", token);
                        $finish;
                    end
                    token = read_token(fp); // skip transaction number
                    token_ingress_status = read_token(fp_ingress_status); // skip transaction number
                    token_ingress_status = read_token(fp_ingress_status); // load ingress status at beginning of transaction
                    fifo_src_axis_TDEST.clear();
                    token = read_token(fp);
                    ret = $sscanf(token_ingress_status, "%d", ingress_status);
                    token_ingress_status = read_token(fp_ingress_status);
                    while (token != "[[/transaction]]") begin
                        if (fifo_src_axis_TDEST.full) begin
                            $display("ERROR: FIFO is full");
                            $finish;
                        end
                        ret = $sscanf(rm_0x(token), "%x", data_integer);
                        data = data_integer;
                        fifo_src_axis_TDEST.push(data);
                        token = read_token(fp);
                        ret = $sscanf(token_ingress_status, "%d", ingress_status);
                        token_ingress_status = read_token(fp_ingress_status);
                    end
                    src_axis_TDEST_valid = (ingress_status > 0);
                    token = read_token(fp);
                    token_ingress_status = read_token(fp_ingress_status);
                    fifo_src_axis_TDEST.snapshot();
                end else begin
                    fifo_src_axis_TDEST.restore();
                    if (fp != 0) begin
                        $fclose(fp);
                        fp = 0;
                    end
                    if (fp_ingress_status != 0) begin
                        $fclose(fp_ingress_status);
                        fp_ingress_status = 0;
                    end
                end
                transaction_load_src_axis_TDEST = transaction_load_src_axis_TDEST + 1;
            end
        end
    end

endmodule
