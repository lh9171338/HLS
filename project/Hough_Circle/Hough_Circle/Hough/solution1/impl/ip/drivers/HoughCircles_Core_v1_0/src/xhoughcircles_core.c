// ==============================================================
// File generated by Vivado(TM) HLS - High-Level Synthesis from C, C++ and SystemC
// Version: 2018.2
// Copyright (C) 1986-2018 Xilinx, Inc. All Rights Reserved.
// 
// ==============================================================

/***************************** Include Files *********************************/
#include "xhoughcircles_core.h"

/************************** Function Implementation *************************/
#ifndef __linux__
int XHoughcircles_core_CfgInitialize(XHoughcircles_core *InstancePtr, XHoughcircles_core_Config *ConfigPtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(ConfigPtr != NULL);

    InstancePtr->Ctrl_BaseAddress = ConfigPtr->Ctrl_BaseAddress;
    InstancePtr->IsReady = XIL_COMPONENT_IS_READY;

    return XST_SUCCESS;
}
#endif

void XHoughcircles_core_Start(XHoughcircles_core *InstancePtr) {
    u32 Data;

    Xil_AssertVoid(InstancePtr != NULL);
    Xil_AssertVoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    Data = XHoughcircles_core_ReadReg(InstancePtr->Ctrl_BaseAddress, XHOUGHCIRCLES_CORE_CTRL_ADDR_AP_CTRL) & 0x80;
    XHoughcircles_core_WriteReg(InstancePtr->Ctrl_BaseAddress, XHOUGHCIRCLES_CORE_CTRL_ADDR_AP_CTRL, Data | 0x01);
}

u32 XHoughcircles_core_IsDone(XHoughcircles_core *InstancePtr) {
    u32 Data;

    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    Data = XHoughcircles_core_ReadReg(InstancePtr->Ctrl_BaseAddress, XHOUGHCIRCLES_CORE_CTRL_ADDR_AP_CTRL);
    return (Data >> 1) & 0x1;
}

u32 XHoughcircles_core_IsIdle(XHoughcircles_core *InstancePtr) {
    u32 Data;

    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    Data = XHoughcircles_core_ReadReg(InstancePtr->Ctrl_BaseAddress, XHOUGHCIRCLES_CORE_CTRL_ADDR_AP_CTRL);
    return (Data >> 2) & 0x1;
}

u32 XHoughcircles_core_IsReady(XHoughcircles_core *InstancePtr) {
    u32 Data;

    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    Data = XHoughcircles_core_ReadReg(InstancePtr->Ctrl_BaseAddress, XHOUGHCIRCLES_CORE_CTRL_ADDR_AP_CTRL);
    // check ap_start to see if the pcore is ready for next input
    return !(Data & 0x1);
}

void XHoughcircles_core_EnableAutoRestart(XHoughcircles_core *InstancePtr) {
    Xil_AssertVoid(InstancePtr != NULL);
    Xil_AssertVoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    XHoughcircles_core_WriteReg(InstancePtr->Ctrl_BaseAddress, XHOUGHCIRCLES_CORE_CTRL_ADDR_AP_CTRL, 0x80);
}

void XHoughcircles_core_DisableAutoRestart(XHoughcircles_core *InstancePtr) {
    Xil_AssertVoid(InstancePtr != NULL);
    Xil_AssertVoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    XHoughcircles_core_WriteReg(InstancePtr->Ctrl_BaseAddress, XHOUGHCIRCLES_CORE_CTRL_ADDR_AP_CTRL, 0);
}

void XHoughcircles_core_Set_rows(XHoughcircles_core *InstancePtr, u32 Data) {
    Xil_AssertVoid(InstancePtr != NULL);
    Xil_AssertVoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    XHoughcircles_core_WriteReg(InstancePtr->Ctrl_BaseAddress, XHOUGHCIRCLES_CORE_CTRL_ADDR_ROWS_DATA, Data);
}

u32 XHoughcircles_core_Get_rows(XHoughcircles_core *InstancePtr) {
    u32 Data;

    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    Data = XHoughcircles_core_ReadReg(InstancePtr->Ctrl_BaseAddress, XHOUGHCIRCLES_CORE_CTRL_ADDR_ROWS_DATA);
    return Data;
}

void XHoughcircles_core_Set_cols(XHoughcircles_core *InstancePtr, u32 Data) {
    Xil_AssertVoid(InstancePtr != NULL);
    Xil_AssertVoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    XHoughcircles_core_WriteReg(InstancePtr->Ctrl_BaseAddress, XHOUGHCIRCLES_CORE_CTRL_ADDR_COLS_DATA, Data);
}

u32 XHoughcircles_core_Get_cols(XHoughcircles_core *InstancePtr) {
    u32 Data;

    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    Data = XHoughcircles_core_ReadReg(InstancePtr->Ctrl_BaseAddress, XHOUGHCIRCLES_CORE_CTRL_ADDR_COLS_DATA);
    return Data;
}

void XHoughcircles_core_Set_gray_thresh(XHoughcircles_core *InstancePtr, u32 Data) {
    Xil_AssertVoid(InstancePtr != NULL);
    Xil_AssertVoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    XHoughcircles_core_WriteReg(InstancePtr->Ctrl_BaseAddress, XHOUGHCIRCLES_CORE_CTRL_ADDR_GRAY_THRESH_DATA, Data);
}

u32 XHoughcircles_core_Get_gray_thresh(XHoughcircles_core *InstancePtr) {
    u32 Data;

    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    Data = XHoughcircles_core_ReadReg(InstancePtr->Ctrl_BaseAddress, XHOUGHCIRCLES_CORE_CTRL_ADDR_GRAY_THRESH_DATA);
    return Data;
}

void XHoughcircles_core_Set_acc_thresh(XHoughcircles_core *InstancePtr, u32 Data) {
    Xil_AssertVoid(InstancePtr != NULL);
    Xil_AssertVoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    XHoughcircles_core_WriteReg(InstancePtr->Ctrl_BaseAddress, XHOUGHCIRCLES_CORE_CTRL_ADDR_ACC_THRESH_DATA, Data);
}

u32 XHoughcircles_core_Get_acc_thresh(XHoughcircles_core *InstancePtr) {
    u32 Data;

    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    Data = XHoughcircles_core_ReadReg(InstancePtr->Ctrl_BaseAddress, XHOUGHCIRCLES_CORE_CTRL_ADDR_ACC_THRESH_DATA);
    return Data;
}

void XHoughcircles_core_Set_min_dist(XHoughcircles_core *InstancePtr, u32 Data) {
    Xil_AssertVoid(InstancePtr != NULL);
    Xil_AssertVoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    XHoughcircles_core_WriteReg(InstancePtr->Ctrl_BaseAddress, XHOUGHCIRCLES_CORE_CTRL_ADDR_MIN_DIST_DATA, Data);
}

u32 XHoughcircles_core_Get_min_dist(XHoughcircles_core *InstancePtr) {
    u32 Data;

    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    Data = XHoughcircles_core_ReadReg(InstancePtr->Ctrl_BaseAddress, XHOUGHCIRCLES_CORE_CTRL_ADDR_MIN_DIST_DATA);
    return Data;
}

void XHoughcircles_core_Set_min_radius(XHoughcircles_core *InstancePtr, u32 Data) {
    Xil_AssertVoid(InstancePtr != NULL);
    Xil_AssertVoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    XHoughcircles_core_WriteReg(InstancePtr->Ctrl_BaseAddress, XHOUGHCIRCLES_CORE_CTRL_ADDR_MIN_RADIUS_DATA, Data);
}

u32 XHoughcircles_core_Get_min_radius(XHoughcircles_core *InstancePtr) {
    u32 Data;

    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    Data = XHoughcircles_core_ReadReg(InstancePtr->Ctrl_BaseAddress, XHOUGHCIRCLES_CORE_CTRL_ADDR_MIN_RADIUS_DATA);
    return Data;
}

void XHoughcircles_core_Set_max_radius(XHoughcircles_core *InstancePtr, u32 Data) {
    Xil_AssertVoid(InstancePtr != NULL);
    Xil_AssertVoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    XHoughcircles_core_WriteReg(InstancePtr->Ctrl_BaseAddress, XHOUGHCIRCLES_CORE_CTRL_ADDR_MAX_RADIUS_DATA, Data);
}

u32 XHoughcircles_core_Get_max_radius(XHoughcircles_core *InstancePtr) {
    u32 Data;

    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    Data = XHoughcircles_core_ReadReg(InstancePtr->Ctrl_BaseAddress, XHOUGHCIRCLES_CORE_CTRL_ADDR_MAX_RADIUS_DATA);
    return Data;
}

void XHoughcircles_core_InterruptGlobalEnable(XHoughcircles_core *InstancePtr) {
    Xil_AssertVoid(InstancePtr != NULL);
    Xil_AssertVoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    XHoughcircles_core_WriteReg(InstancePtr->Ctrl_BaseAddress, XHOUGHCIRCLES_CORE_CTRL_ADDR_GIE, 1);
}

void XHoughcircles_core_InterruptGlobalDisable(XHoughcircles_core *InstancePtr) {
    Xil_AssertVoid(InstancePtr != NULL);
    Xil_AssertVoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    XHoughcircles_core_WriteReg(InstancePtr->Ctrl_BaseAddress, XHOUGHCIRCLES_CORE_CTRL_ADDR_GIE, 0);
}

void XHoughcircles_core_InterruptEnable(XHoughcircles_core *InstancePtr, u32 Mask) {
    u32 Register;

    Xil_AssertVoid(InstancePtr != NULL);
    Xil_AssertVoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    Register =  XHoughcircles_core_ReadReg(InstancePtr->Ctrl_BaseAddress, XHOUGHCIRCLES_CORE_CTRL_ADDR_IER);
    XHoughcircles_core_WriteReg(InstancePtr->Ctrl_BaseAddress, XHOUGHCIRCLES_CORE_CTRL_ADDR_IER, Register | Mask);
}

void XHoughcircles_core_InterruptDisable(XHoughcircles_core *InstancePtr, u32 Mask) {
    u32 Register;

    Xil_AssertVoid(InstancePtr != NULL);
    Xil_AssertVoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    Register =  XHoughcircles_core_ReadReg(InstancePtr->Ctrl_BaseAddress, XHOUGHCIRCLES_CORE_CTRL_ADDR_IER);
    XHoughcircles_core_WriteReg(InstancePtr->Ctrl_BaseAddress, XHOUGHCIRCLES_CORE_CTRL_ADDR_IER, Register & (~Mask));
}

void XHoughcircles_core_InterruptClear(XHoughcircles_core *InstancePtr, u32 Mask) {
    Xil_AssertVoid(InstancePtr != NULL);
    Xil_AssertVoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    XHoughcircles_core_WriteReg(InstancePtr->Ctrl_BaseAddress, XHOUGHCIRCLES_CORE_CTRL_ADDR_ISR, Mask);
}

u32 XHoughcircles_core_InterruptGetEnabled(XHoughcircles_core *InstancePtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    return XHoughcircles_core_ReadReg(InstancePtr->Ctrl_BaseAddress, XHOUGHCIRCLES_CORE_CTRL_ADDR_IER);
}

u32 XHoughcircles_core_InterruptGetStatus(XHoughcircles_core *InstancePtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    return XHoughcircles_core_ReadReg(InstancePtr->Ctrl_BaseAddress, XHOUGHCIRCLES_CORE_CTRL_ADDR_ISR);
}

