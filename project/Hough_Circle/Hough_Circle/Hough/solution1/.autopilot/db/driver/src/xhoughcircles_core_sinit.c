// ==============================================================
// File generated by Vivado(TM) HLS - High-Level Synthesis from C, C++ and SystemC
// Version: 2018.2
// Copyright (C) 1986-2018 Xilinx, Inc. All Rights Reserved.
// 
// ==============================================================

#ifndef __linux__

#include "xstatus.h"
#include "xparameters.h"
#include "xhoughcircles_core.h"

extern XHoughcircles_core_Config XHoughcircles_core_ConfigTable[];

XHoughcircles_core_Config *XHoughcircles_core_LookupConfig(u16 DeviceId) {
	XHoughcircles_core_Config *ConfigPtr = NULL;

	int Index;

	for (Index = 0; Index < XPAR_XHOUGHCIRCLES_CORE_NUM_INSTANCES; Index++) {
		if (XHoughcircles_core_ConfigTable[Index].DeviceId == DeviceId) {
			ConfigPtr = &XHoughcircles_core_ConfigTable[Index];
			break;
		}
	}

	return ConfigPtr;
}

int XHoughcircles_core_Initialize(XHoughcircles_core *InstancePtr, u16 DeviceId) {
	XHoughcircles_core_Config *ConfigPtr;

	Xil_AssertNonvoid(InstancePtr != NULL);

	ConfigPtr = XHoughcircles_core_LookupConfig(DeviceId);
	if (ConfigPtr == NULL) {
		InstancePtr->IsReady = 0;
		return (XST_DEVICE_NOT_FOUND);
	}

	return XHoughcircles_core_CfgInitialize(InstancePtr, ConfigPtr);
}

#endif

